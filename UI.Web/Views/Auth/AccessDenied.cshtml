@{
    ViewData["Title"] = "Acceso Denegado";
}

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-6">
     <div class="card border-danger shadow">
       <div class="card-header bg-danger text-white text-center">
          <h3><i class="bi bi-shield-exclamation"></i> Acceso Denegado</h3>
       </div>
             <div class="card-body text-center">
                    <p class="lead">No tienes permisos para acceder a esta sección.</p>
            <p class="text-muted">Si crees que esto es un error, por favor contacta al administrador.</p>
                <div class="mt-4">
  <a asp-controller="Home" asp-action="Index" class="btn btn-primary me-2">Ir al Inicio</a>
   <a asp-controller="Auth" asp-action="Logout" class="btn btn-secondary">Cerrar Sesión</a>
</div>
       </div>
        </div>
        </div>
    </div>
</div>

<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css">
